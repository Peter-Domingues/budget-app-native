import { StyleSheet } from "react-native";

const SpendingStyles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    backgroundColor: "#e0efdd",
  },
  max: {
    maxHeight: "80vh",
    width: "100%",
  },
});

export default SpendingStyles;
