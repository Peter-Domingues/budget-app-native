import { StyleSheet } from "react-native";

const IncomingStyles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    backgroundColor: "#e0efdd",
  },
  view: { padding: 15, width: "100%" },
  max: {
    maxHeight: "80vh",
    width: "100%",
  },
});

export default IncomingStyles;
