import { StyleSheet } from "react-native";

const AppCalculatorSyles = StyleSheet.create({
  calcWrapper: {
    borderRadius: 20,
    maxWidth: "80%",
  },
  line: { display: "flex", flexDirection: "row", width: "80%" },
});

export default AppCalculatorSyles;
